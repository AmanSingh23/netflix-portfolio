# Netlify configuration for Angular SPA
[build]
  command = "npm run build"
  publish = "dist/netflix-portfolio"

# Redirect all routes to index.html for Angular routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
